{
  "name": "photospeak-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node.js (Hono) proxy that fronts MiMo / Aliyun DashScope so API keys never ship inside the mobile binary.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "dotenv": "^16.4.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
