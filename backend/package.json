{
  "name": "photospeak-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies PhotoSpeak's calls to MiMo / Aliyun DashScope so API keys never ship in the mobile app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.95.0"
  }
}
